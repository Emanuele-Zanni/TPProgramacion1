# Comandos para hacer PUSH
(verificar si es necesario hacer un git pull, sino no va a dejar hacer "git push")
git add . 
git commit -m "Breve descripcion de los cambios realizados"
git push

# Comando para revertir cambios hasta el ULTIMO COMMIT
git reset --hard HEAD
